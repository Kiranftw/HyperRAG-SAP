[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hyperrag-sap"
version = "0.1.0"

[tool.setuptools]
packages = ["agents", "tools", "rag", "prompts"]